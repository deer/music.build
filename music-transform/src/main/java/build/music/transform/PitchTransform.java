package build.music.transform;

import build.music.pitch.Pitch;

/** Marker interface for transforms that operate on individual pitches. */
public interface PitchTransform extends Transform<Pitch> {
}
